# 全栈 AI 应用开发学习计划・学习笔记

> 日期：2026-09-17（周四）｜学习日：D1（合并加速版）
> 阶段：阶段一「编程与工程基础」｜节点：N1.1 Python 基础语法
> 主题：环境搭建 + Python 起步（变量 / 类型 / 输入输出）

---

## 一、学习目标

1. 什么是 venv？为什么每个项目都要建一个？能一条命令建好并激活吗？

2. int /float/str /bool 四种类型怎么互相转换？`input()` 读进来的一定是什么类型？

3. f-string 怎么写？`print()` 怎么排版输出？中文不乱码的条件是什么？

---

## 二、今日任务清单（全部完成 ✅）

| #   | 任务                                                          | 状态 |
| --- | ------------------------------------------------------------- | ---- |
| 1   | 确认 Python 版本 ≥ 3.10（`python --version`）                 | ✅   |
| 2   | 建练习项目目录，创建并激活虚拟环境（`python -m venv .venv`）  | ✅   |
| 3   | 装好编辑器（VS Code + Python 扩展，或 PyCharm），中文正常显示 | ✅   |
| 4   | 写并运行第一个脚本 hello.py，输出「Hello, 全栈 AI!」          | ✅   |
| 5   | 过一遍变量 / 类型 / 输入输出（官方教程前 3 节）               | ✅   |
| 6   | 完成 3 道作业 + 4 道自测题                                    | ✅   |
| 7   | 建 GitHub 仓库 exercises，提交 4 个文件（含 .gitignore）      | ✅   |

---

## 三、知识点总结

### 3.1 虚拟环境 venv

- **是什么**：Python 自带的虚拟环境工具，为每个项目创建独立的包环境。

- **为什么用**：不同项目依赖的包版本互不干扰，避免污染系统 Python，也避免系统装包权限问题。

- **命令**（Windows / PowerShell）：

```
\# 创建（在项目目录下）

python -m venv .venv

\# 激活

.venv\Scripts\Activate.ps1

\# 验证：命令行前面出现 (.venv) 即成功

python --version
```

> ⚠️ 装任何包（pip install）都必须在激活状态下执行。

### 3.2 变量与数据类型

| 类型    | 说明   | 示例             | 转换函数   |
| ------- | ------ | ---------------- | ---------- |
| `int`   | 整数   | `25`             | `int(x)`   |
| `float` | 浮点数 | `25.0`           | `float(x)` |
| `str`   | 字符串 | `"深圳"`         | `str(x)`   |
| `bool`  | 布尔   | `True` / `False` | `bool(x)`  |

**核心规则**：

- 变量命名：字母 / 下划线开头，不能数字开头，不用关键字。

- `input()` 返回的**永远是字符串**，做数学运算前必须转换。

- `bool()` 转换：空值（0、""、None、空容器）为 `False`，其余为 `True`。

### 3.3 输入输出与 f-string

```
name = input("请输入姓名：")        # input 一定返回 str

age = int(input("请输入年龄："))     # 需要数字时手动转换

\# f-string：{} 内直接放变量或表达式

print(f"我是{name}，今年{age}岁，来自深圳，喜欢写代码")

\# 字面花括号用双写转义

print(f"输出一对花括号：{{}}")       # -> 输出一对花括号：{}
```

**中文不乱码的条件**：Python 3 默认 UTF-8，关键是**编辑器保存文件时选 UTF-8 编码**。

---

## 四、测试作业（3 道）与参考实现

### 作业 1・环境验证

在已激活的 venv 里运行 hello.py，输出「Hello, 全栈 AI!」。

```
\# hello.py

print("Hello, 全栈 AI!")
```

验收：`python hello.py` → `Hello, 全栈 AI!`

### 作业 2・个人名片小程序

```
\# card.py

name = "小明"

age = 25

city = "深圳"

hobby = input("请输入你的爱好：")

print(f"我是{name}，今年{age}岁，来自{city}，喜欢{hobby}")
```

验收：运行后按提示输入爱好，输出一行完整自我介绍。

### 作业 3・温度转换器

```
\# temp.py

c = float(input("请输入摄氏温度："))   # 必须 float()，否则 "25" \* 1.8 报错

f = c \* 1.8 + 32

print(f"{f:.1f}°F")                    # 保留 1 位小数
```

验收：输入 `25` → 输出 `77.0°F`。

---

## 五、自测 4 题与答案

| #   | 题目                              | 答案                                                                 |
| --- | --------------------------------- | -------------------------------------------------------------------- |
| ①   | `"10" + 5` 会报错还是输出 15？    | **报错**。str 和 int 不能直接相加，需先 `int("10")` 或 `str(5)`      |
| ②   | `bool(0)` 和 `bool("0")` 分别是？ | `bool(0)` = **False**；`bool("0")` = **True**（非空字符串都为 True） |
| ③   | `x = 5`，`if x = 5:` 合法吗？     | **不合法**。`=` 是赋值，条件判断要用 `==`，直接语法错误              |
| ④   | f-string 里输出字面花括号 `{}`？  | 写两个花括号 `{{}}`                                                  |

---

## 六、易错点（今日踩坑记录）

1. **venv 没激活就装包** → 包装进了系统 Python，项目间互相污染。

2. **input () 忘记类型转换** → `"25" * 1.8` 直接 TypeError。

3. `=`**&#x20;与&#x20;**`==`**&#x20;混用** → 赋值和比较不分，语法错误。

4. **中文乱码** → 编辑器保存编码不是 UTF-8。

5. **类型混加** → str 和 int 不能直接 `+`。

---

## 七、完成标准确认

- [x] 3 道作业全部能运行，输出符合预期

- [x] 自测 4 题答对 3 题以上

- [x] 所有代码运行在 venv 内，未污染系统 Python

- [x] exercises 仓库已创建，4 个文件已提交（含 .gitignore）

---

## 八、学习资源

- Python 官方教程：[https://docs.python.org/zh-cn/3/tutorial/introduction.html](https://docs.python.org/zh-cn/3/tutorial/introduction.html)

- 廖雪峰 Python 教程：[https://liaoxuefeng.com/books/python/](https://liaoxuefeng.com/books/python/)

---

## 九、下一步预告

- **明日（2026-09-18，周五 08:00）D3・条件分支与循环**：`if/elif/else`、`for/while`、`break/continue`

- 进度回执：如提前完成可回复「已完成到 D3」，次日推送自动跳到 D4
